_target_: rectified_point_flow.data.PointCloudDataModule
num_points_to_sample: 5000
min_parts: 2
max_parts: 64
min_points_per_part: 20
multi_ref: true

dataset_paths:
  ikea: "${data_root}/ikea.hdf5"
dataset_names: ["ikea"]

batch_size: 8
num_workers: 8